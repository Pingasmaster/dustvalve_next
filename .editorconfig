root = true

[*.{kt,kts}]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 4
insert_final_newline = true
max_line_length = 140
ij_kotlin_allow_trailing_comma = true
ij_kotlin_allow_trailing_comma_on_call_site = true
ktlint_code_style = intellij_idea
# @Composable PascalCase is standard; keep naming for non-composables.
ktlint_function_naming_ignore_when_annotated_with = Composable
# Compose theme / CompositionLocal conventions (LocalFoo, AppTypography) clash
# with lowerCamelCase property naming; keep disabled for those conventions.
ktlint_standard_property-naming = disabled
# StateFlow `_state` / `state.asStateFlow()` pattern is standard; ktlint's
# backing-property-naming check rejects it.
ktlint_standard_backing-property-naming = disabled
# Multi-declaration files (e.g. Shape.kt hosting AppShapes) are intentional;
# filename matching would force artificial one-type-per-file churn.
ktlint_standard_filename = disabled

# Tests often embed long JSON fixtures; lift the line-length cap there.
[**/src/test/**.{kt,kts}]
max_line_length = off
ktlint_standard_max-line-length = disabled

[**/src/androidTest/**.{kt,kts}]
max_line_length = off
ktlint_standard_max-line-length = disabled
